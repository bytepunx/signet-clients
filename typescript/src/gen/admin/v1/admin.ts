// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               unknown
// source: admin/v1/admin.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type ClientWritableStream,
  type handleClientStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "admin.v1";

export interface UnsealKeyRequest {
  key: Uint8Array;
}

export interface UnsealKeyResponse {
  unsealed: boolean;
  sharesReceived: number;
  sharesRequired: number;
  message: string;
}

export interface UnsealShareRequest {
  share: Uint8Array;
}

export interface UnsealShareResponse {
  unsealed: boolean;
  sharesReceived: number;
  sharesRequired: number;
  message: string;
}

export interface SealRequest {
}

export interface SealResponse {
  message: string;
}

export interface StatusRequest {
}

export interface StatusResponse {
  state: StatusResponse_State;
  sharesReceived: number;
  sharesRequired: number;
  version: string;
}

export enum StatusResponse_State {
  STATE_UNSPECIFIED = 0,
  STATE_SEALED = 1,
  STATE_UNSEALING = 2,
  STATE_UNSEALED = 3,
  UNRECOGNIZED = -1,
}

export function statusResponse_StateFromJSON(object: any): StatusResponse_State {
  switch (object) {
    case 0:
    case "STATE_UNSPECIFIED":
      return StatusResponse_State.STATE_UNSPECIFIED;
    case 1:
    case "STATE_SEALED":
      return StatusResponse_State.STATE_SEALED;
    case 2:
    case "STATE_UNSEALING":
      return StatusResponse_State.STATE_UNSEALING;
    case 3:
    case "STATE_UNSEALED":
      return StatusResponse_State.STATE_UNSEALED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StatusResponse_State.UNRECOGNIZED;
  }
}

export function statusResponse_StateToJSON(object: StatusResponse_State): string {
  switch (object) {
    case StatusResponse_State.STATE_UNSPECIFIED:
      return "STATE_UNSPECIFIED";
    case StatusResponse_State.STATE_SEALED:
      return "STATE_SEALED";
    case StatusResponse_State.STATE_UNSEALING:
      return "STATE_UNSEALING";
    case StatusResponse_State.STATE_UNSEALED:
      return "STATE_UNSEALED";
    case StatusResponse_State.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface RotateKEKRequest {
}

export interface RotateKEKResponse {
  newKekId: string;
  /**
   * old_kek_id is retained (deactivated, not deleted) so DEKs wrapped before
   * rotation completes remain decryptable until PruneKEK is called.
   */
  oldKekId: string;
  /**
   * secrets_rewrapped is the number of DEKs successfully re-wrapped under
   * the new KEK during this call.
   */
  secretsRewrapped: number;
}

export interface ListKEKsRequest {
}

export interface KEKInfo {
  id: string;
  isActive: boolean;
  createdAt: string;
  /** empty if still active */
  deactivatedAt: string;
}

export interface ListKEKsResponse {
  keks: KEKInfo[];
}

export interface PruneKEKRequest {
  id: string;
}

export interface PruneKEKResponse {
  message: string;
}

export interface RotateMasterKeyRequest {
  /** new_key must be exactly 32 bytes. */
  newKey: Uint8Array;
}

export interface RotateMasterKeyResponse {
  message: string;
  keksRewrapped: number;
}

export interface GetSOPSPublicKeyRequest {
}

export interface GetSOPSPublicKeyResponse {
  /** age public key in bech32 format (age1...). Add to .sops.yaml as an age recipient. */
  publicKey: string;
  fingerprint: string;
  createdAt: string;
  /** environment this key is scoped to; empty = global (usable by any environment). */
  environment: string;
}

export interface RotateSOPSKeyRequest {
}

export interface RotateSOPSKeyResponse {
  newPublicKey: string;
  newFingerprint: string;
  /** old_public_key is retained for decryption until pruned. */
  oldPublicKey: string;
  /** environment the new key is scoped to; empty = global. */
  newEnvironment: string;
}

export interface ListSOPSKeysRequest {
}

export interface SOPSKeyInfo {
  publicKey: string;
  fingerprint: string;
  isActive: boolean;
  createdAt: string;
  /** empty if still active */
  deactivatedAt: string;
  /** environment this key is scoped to; empty = global. */
  environment: string;
}

export interface ListSOPSKeysResponse {
  keys: SOPSKeyInfo[];
}

export interface PruneSOPSKeyRequest {
  publicKey: string;
}

export interface PruneSOPSKeyResponse {
  message: string;
}

export interface RegisterRepositoryRequest {
  /** human alias, e.g. "infra-secrets" */
  name: string;
  /** https://github.com/org/repo */
  repoUrl: string;
  /** e.g. "main" */
  branch: string;
  /** path within repo, e.g. "secrets/" */
  secretsPath: string;
  /** PEM-encoded SSH private key for read-only deploy access. */
  deployKey: Uint8Array;
  /**
   * config_path is the directory prefix within the repo that holds plain YAML
   * config files, e.g. "config/". Leave empty to disable config sync.
   */
  configPath: string;
}

export interface RegisterRepositoryResponse {
  id: string;
  /** webhook_url is the endpoint to configure in GitHub (Settings → Webhooks). */
  webhookUrl: string;
  /**
   * webhook_secret is the shared HMAC secret to configure in GitHub.
   * Shown only on registration; not retrievable afterwards.
   */
  webhookSecret: string;
}

export interface ListRepositoriesRequest {
}

export interface RepositoryInfo {
  id: string;
  name: string;
  repoUrl: string;
  branch: string;
  secretsPath: string;
  lastSyncSha: string;
  lastSyncAt: string;
  configPath: string;
}

export interface ListRepositoriesResponse {
  repositories: RepositoryInfo[];
}

export interface RemoveRepositoryRequest {
  id: string;
}

export interface RemoveRepositoryResponse {
  message: string;
}

export interface TriggerSyncRequest {
  /** repository id */
  id: string;
}

export interface TriggerSyncResponse {
  secretsAdded: number;
  secretsUpdated: number;
  secretsDeleted: number;
  syncSha: string;
  errors: string[];
  configsSynced: number;
}

/**
 * SyncBundleChunk is a single streaming message for the SyncBundle RPC.
 * The first message must contain a SyncBundleHeader; all subsequent messages
 * must contain raw tar.gz archive bytes.
 */
export interface SyncBundleChunk {
  header?: SyncBundleHeader | undefined;
  data?: Uint8Array | undefined;
}

/** SyncBundleHeader carries metadata sent as the first SyncBundleChunk. */
export interface SyncBundleHeader {
  /**
   * secrets_path is the directory prefix within the repo that holds secrets,
   * e.g. "secrets/". Defaults to "secrets/" if empty.
   */
  secretsPath: string;
  /**
   * head_sha is the HEAD commit SHA of the local repo, used for audit logging.
   * May be empty for non-git sources.
   */
  headSha: string;
  /**
   * config_path is the directory prefix for plain YAML config files, e.g. "config/".
   * Leave empty to skip config sync.
   */
  configPath: string;
}

export interface SyncBundleResponse {
  secretsAdded: number;
  secretsUpdated: number;
  secretsDeleted: number;
  syncSha: string;
  errors: string[];
  configsSynced: number;
}

function createBaseUnsealKeyRequest(): UnsealKeyRequest {
  return { key: new Uint8Array(0) };
}

export const UnsealKeyRequest: MessageFns<UnsealKeyRequest> = {
  encode(message: UnsealKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key.length !== 0) {
      writer.uint32(10).bytes(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsealKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsealKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsealKeyRequest {
    return { key: isSet(object.key) ? bytesFromBase64(object.key) : new Uint8Array(0) };
  },

  toJSON(message: UnsealKeyRequest): unknown {
    const obj: any = {};
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    return obj;
  },

  create(base?: DeepPartial<UnsealKeyRequest>): UnsealKeyRequest {
    return UnsealKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnsealKeyRequest>): UnsealKeyRequest {
    const message = createBaseUnsealKeyRequest();
    message.key = object.key ?? new Uint8Array(0);
    return message;
  },
};

function createBaseUnsealKeyResponse(): UnsealKeyResponse {
  return { unsealed: false, sharesReceived: 0, sharesRequired: 0, message: "" };
}

export const UnsealKeyResponse: MessageFns<UnsealKeyResponse> = {
  encode(message: UnsealKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.unsealed !== false) {
      writer.uint32(8).bool(message.unsealed);
    }
    if (message.sharesReceived !== 0) {
      writer.uint32(16).int32(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      writer.uint32(24).int32(message.sharesRequired);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsealKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsealKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.unsealed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sharesReceived = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sharesRequired = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsealKeyResponse {
    return {
      unsealed: isSet(object.unsealed) ? globalThis.Boolean(object.unsealed) : false,
      sharesReceived: isSet(object.sharesReceived)
        ? globalThis.Number(object.sharesReceived)
        : isSet(object.shares_received)
        ? globalThis.Number(object.shares_received)
        : 0,
      sharesRequired: isSet(object.sharesRequired)
        ? globalThis.Number(object.sharesRequired)
        : isSet(object.shares_required)
        ? globalThis.Number(object.shares_required)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: UnsealKeyResponse): unknown {
    const obj: any = {};
    if (message.unsealed !== false) {
      obj.unsealed = message.unsealed;
    }
    if (message.sharesReceived !== 0) {
      obj.sharesReceived = Math.round(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      obj.sharesRequired = Math.round(message.sharesRequired);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<UnsealKeyResponse>): UnsealKeyResponse {
    return UnsealKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnsealKeyResponse>): UnsealKeyResponse {
    const message = createBaseUnsealKeyResponse();
    message.unsealed = object.unsealed ?? false;
    message.sharesReceived = object.sharesReceived ?? 0;
    message.sharesRequired = object.sharesRequired ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseUnsealShareRequest(): UnsealShareRequest {
  return { share: new Uint8Array(0) };
}

export const UnsealShareRequest: MessageFns<UnsealShareRequest> = {
  encode(message: UnsealShareRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.share.length !== 0) {
      writer.uint32(10).bytes(message.share);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsealShareRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsealShareRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.share = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsealShareRequest {
    return { share: isSet(object.share) ? bytesFromBase64(object.share) : new Uint8Array(0) };
  },

  toJSON(message: UnsealShareRequest): unknown {
    const obj: any = {};
    if (message.share.length !== 0) {
      obj.share = base64FromBytes(message.share);
    }
    return obj;
  },

  create(base?: DeepPartial<UnsealShareRequest>): UnsealShareRequest {
    return UnsealShareRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnsealShareRequest>): UnsealShareRequest {
    const message = createBaseUnsealShareRequest();
    message.share = object.share ?? new Uint8Array(0);
    return message;
  },
};

function createBaseUnsealShareResponse(): UnsealShareResponse {
  return { unsealed: false, sharesReceived: 0, sharesRequired: 0, message: "" };
}

export const UnsealShareResponse: MessageFns<UnsealShareResponse> = {
  encode(message: UnsealShareResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.unsealed !== false) {
      writer.uint32(8).bool(message.unsealed);
    }
    if (message.sharesReceived !== 0) {
      writer.uint32(16).int32(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      writer.uint32(24).int32(message.sharesRequired);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnsealShareResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnsealShareResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.unsealed = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sharesReceived = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sharesRequired = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnsealShareResponse {
    return {
      unsealed: isSet(object.unsealed) ? globalThis.Boolean(object.unsealed) : false,
      sharesReceived: isSet(object.sharesReceived)
        ? globalThis.Number(object.sharesReceived)
        : isSet(object.shares_received)
        ? globalThis.Number(object.shares_received)
        : 0,
      sharesRequired: isSet(object.sharesRequired)
        ? globalThis.Number(object.sharesRequired)
        : isSet(object.shares_required)
        ? globalThis.Number(object.shares_required)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: UnsealShareResponse): unknown {
    const obj: any = {};
    if (message.unsealed !== false) {
      obj.unsealed = message.unsealed;
    }
    if (message.sharesReceived !== 0) {
      obj.sharesReceived = Math.round(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      obj.sharesRequired = Math.round(message.sharesRequired);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<UnsealShareResponse>): UnsealShareResponse {
    return UnsealShareResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UnsealShareResponse>): UnsealShareResponse {
    const message = createBaseUnsealShareResponse();
    message.unsealed = object.unsealed ?? false;
    message.sharesReceived = object.sharesReceived ?? 0;
    message.sharesRequired = object.sharesRequired ?? 0;
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseSealRequest(): SealRequest {
  return {};
}

export const SealRequest: MessageFns<SealRequest> = {
  encode(_: SealRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SealRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSealRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SealRequest {
    return {};
  },

  toJSON(_: SealRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SealRequest>): SealRequest {
    return SealRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SealRequest>): SealRequest {
    const message = createBaseSealRequest();
    return message;
  },
};

function createBaseSealResponse(): SealResponse {
  return { message: "" };
}

export const SealResponse: MessageFns<SealResponse> = {
  encode(message: SealResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SealResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSealResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SealResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: SealResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<SealResponse>): SealResponse {
    return SealResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SealResponse>): SealResponse {
    const message = createBaseSealResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseStatusRequest(): StatusRequest {
  return {};
}

export const StatusRequest: MessageFns<StatusRequest> = {
  encode(_: StatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StatusRequest {
    return {};
  },

  toJSON(_: StatusRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<StatusRequest>): StatusRequest {
    return StatusRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<StatusRequest>): StatusRequest {
    const message = createBaseStatusRequest();
    return message;
  },
};

function createBaseStatusResponse(): StatusResponse {
  return { state: 0, sharesReceived: 0, sharesRequired: 0, version: "" };
}

export const StatusResponse: MessageFns<StatusResponse> = {
  encode(message: StatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state !== 0) {
      writer.uint32(8).int32(message.state);
    }
    if (message.sharesReceived !== 0) {
      writer.uint32(16).int32(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      writer.uint32(24).int32(message.sharesRequired);
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.state = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sharesReceived = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sharesRequired = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusResponse {
    return {
      state: isSet(object.state) ? statusResponse_StateFromJSON(object.state) : 0,
      sharesReceived: isSet(object.sharesReceived)
        ? globalThis.Number(object.sharesReceived)
        : isSet(object.shares_received)
        ? globalThis.Number(object.shares_received)
        : 0,
      sharesRequired: isSet(object.sharesRequired)
        ? globalThis.Number(object.sharesRequired)
        : isSet(object.shares_required)
        ? globalThis.Number(object.shares_required)
        : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: StatusResponse): unknown {
    const obj: any = {};
    if (message.state !== 0) {
      obj.state = statusResponse_StateToJSON(message.state);
    }
    if (message.sharesReceived !== 0) {
      obj.sharesReceived = Math.round(message.sharesReceived);
    }
    if (message.sharesRequired !== 0) {
      obj.sharesRequired = Math.round(message.sharesRequired);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create(base?: DeepPartial<StatusResponse>): StatusResponse {
    return StatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusResponse>): StatusResponse {
    const message = createBaseStatusResponse();
    message.state = object.state ?? 0;
    message.sharesReceived = object.sharesReceived ?? 0;
    message.sharesRequired = object.sharesRequired ?? 0;
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseRotateKEKRequest(): RotateKEKRequest {
  return {};
}

export const RotateKEKRequest: MessageFns<RotateKEKRequest> = {
  encode(_: RotateKEKRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateKEKRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateKEKRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RotateKEKRequest {
    return {};
  },

  toJSON(_: RotateKEKRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RotateKEKRequest>): RotateKEKRequest {
    return RotateKEKRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RotateKEKRequest>): RotateKEKRequest {
    const message = createBaseRotateKEKRequest();
    return message;
  },
};

function createBaseRotateKEKResponse(): RotateKEKResponse {
  return { newKekId: "", oldKekId: "", secretsRewrapped: 0 };
}

export const RotateKEKResponse: MessageFns<RotateKEKResponse> = {
  encode(message: RotateKEKResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newKekId !== "") {
      writer.uint32(10).string(message.newKekId);
    }
    if (message.oldKekId !== "") {
      writer.uint32(18).string(message.oldKekId);
    }
    if (message.secretsRewrapped !== 0) {
      writer.uint32(24).int32(message.secretsRewrapped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateKEKResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateKEKResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newKekId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.oldKekId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.secretsRewrapped = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateKEKResponse {
    return {
      newKekId: isSet(object.newKekId)
        ? globalThis.String(object.newKekId)
        : isSet(object.new_kek_id)
        ? globalThis.String(object.new_kek_id)
        : "",
      oldKekId: isSet(object.oldKekId)
        ? globalThis.String(object.oldKekId)
        : isSet(object.old_kek_id)
        ? globalThis.String(object.old_kek_id)
        : "",
      secretsRewrapped: isSet(object.secretsRewrapped)
        ? globalThis.Number(object.secretsRewrapped)
        : isSet(object.secrets_rewrapped)
        ? globalThis.Number(object.secrets_rewrapped)
        : 0,
    };
  },

  toJSON(message: RotateKEKResponse): unknown {
    const obj: any = {};
    if (message.newKekId !== "") {
      obj.newKekId = message.newKekId;
    }
    if (message.oldKekId !== "") {
      obj.oldKekId = message.oldKekId;
    }
    if (message.secretsRewrapped !== 0) {
      obj.secretsRewrapped = Math.round(message.secretsRewrapped);
    }
    return obj;
  },

  create(base?: DeepPartial<RotateKEKResponse>): RotateKEKResponse {
    return RotateKEKResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RotateKEKResponse>): RotateKEKResponse {
    const message = createBaseRotateKEKResponse();
    message.newKekId = object.newKekId ?? "";
    message.oldKekId = object.oldKekId ?? "";
    message.secretsRewrapped = object.secretsRewrapped ?? 0;
    return message;
  },
};

function createBaseListKEKsRequest(): ListKEKsRequest {
  return {};
}

export const ListKEKsRequest: MessageFns<ListKEKsRequest> = {
  encode(_: ListKEKsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKEKsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKEKsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListKEKsRequest {
    return {};
  },

  toJSON(_: ListKEKsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListKEKsRequest>): ListKEKsRequest {
    return ListKEKsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListKEKsRequest>): ListKEKsRequest {
    const message = createBaseListKEKsRequest();
    return message;
  },
};

function createBaseKEKInfo(): KEKInfo {
  return { id: "", isActive: false, createdAt: "", deactivatedAt: "" };
}

export const KEKInfo: MessageFns<KEKInfo> = {
  encode(message: KEKInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.isActive !== false) {
      writer.uint32(16).bool(message.isActive);
    }
    if (message.createdAt !== "") {
      writer.uint32(26).string(message.createdAt);
    }
    if (message.deactivatedAt !== "") {
      writer.uint32(34).string(message.deactivatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KEKInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKEKInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.deactivatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KEKInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      isActive: isSet(object.isActive)
        ? globalThis.Boolean(object.isActive)
        : isSet(object.is_active)
        ? globalThis.Boolean(object.is_active)
        : false,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      deactivatedAt: isSet(object.deactivatedAt)
        ? globalThis.String(object.deactivatedAt)
        : isSet(object.deactivated_at)
        ? globalThis.String(object.deactivated_at)
        : "",
    };
  },

  toJSON(message: KEKInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.deactivatedAt !== "") {
      obj.deactivatedAt = message.deactivatedAt;
    }
    return obj;
  },

  create(base?: DeepPartial<KEKInfo>): KEKInfo {
    return KEKInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KEKInfo>): KEKInfo {
    const message = createBaseKEKInfo();
    message.id = object.id ?? "";
    message.isActive = object.isActive ?? false;
    message.createdAt = object.createdAt ?? "";
    message.deactivatedAt = object.deactivatedAt ?? "";
    return message;
  },
};

function createBaseListKEKsResponse(): ListKEKsResponse {
  return { keks: [] };
}

export const ListKEKsResponse: MessageFns<ListKEKsResponse> = {
  encode(message: ListKEKsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keks) {
      KEKInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListKEKsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListKEKsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keks.push(KEKInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListKEKsResponse {
    return { keks: globalThis.Array.isArray(object?.keks) ? object.keks.map((e: any) => KEKInfo.fromJSON(e)) : [] };
  },

  toJSON(message: ListKEKsResponse): unknown {
    const obj: any = {};
    if (message.keks?.length) {
      obj.keks = message.keks.map((e) => KEKInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListKEKsResponse>): ListKEKsResponse {
    return ListKEKsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListKEKsResponse>): ListKEKsResponse {
    const message = createBaseListKEKsResponse();
    message.keks = object.keks?.map((e) => KEKInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBasePruneKEKRequest(): PruneKEKRequest {
  return { id: "" };
}

export const PruneKEKRequest: MessageFns<PruneKEKRequest> = {
  encode(message: PruneKEKRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PruneKEKRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePruneKEKRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PruneKEKRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: PruneKEKRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<PruneKEKRequest>): PruneKEKRequest {
    return PruneKEKRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PruneKEKRequest>): PruneKEKRequest {
    const message = createBasePruneKEKRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBasePruneKEKResponse(): PruneKEKResponse {
  return { message: "" };
}

export const PruneKEKResponse: MessageFns<PruneKEKResponse> = {
  encode(message: PruneKEKResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PruneKEKResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePruneKEKResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PruneKEKResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: PruneKEKResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<PruneKEKResponse>): PruneKEKResponse {
    return PruneKEKResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PruneKEKResponse>): PruneKEKResponse {
    const message = createBasePruneKEKResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRotateMasterKeyRequest(): RotateMasterKeyRequest {
  return { newKey: new Uint8Array(0) };
}

export const RotateMasterKeyRequest: MessageFns<RotateMasterKeyRequest> = {
  encode(message: RotateMasterKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newKey.length !== 0) {
      writer.uint32(10).bytes(message.newKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateMasterKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateMasterKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateMasterKeyRequest {
    return {
      newKey: isSet(object.newKey)
        ? bytesFromBase64(object.newKey)
        : isSet(object.new_key)
        ? bytesFromBase64(object.new_key)
        : new Uint8Array(0),
    };
  },

  toJSON(message: RotateMasterKeyRequest): unknown {
    const obj: any = {};
    if (message.newKey.length !== 0) {
      obj.newKey = base64FromBytes(message.newKey);
    }
    return obj;
  },

  create(base?: DeepPartial<RotateMasterKeyRequest>): RotateMasterKeyRequest {
    return RotateMasterKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RotateMasterKeyRequest>): RotateMasterKeyRequest {
    const message = createBaseRotateMasterKeyRequest();
    message.newKey = object.newKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRotateMasterKeyResponse(): RotateMasterKeyResponse {
  return { message: "", keksRewrapped: 0 };
}

export const RotateMasterKeyResponse: MessageFns<RotateMasterKeyResponse> = {
  encode(message: RotateMasterKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.keksRewrapped !== 0) {
      writer.uint32(16).int32(message.keksRewrapped);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateMasterKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateMasterKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.keksRewrapped = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateMasterKeyResponse {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      keksRewrapped: isSet(object.keksRewrapped)
        ? globalThis.Number(object.keksRewrapped)
        : isSet(object.keks_rewrapped)
        ? globalThis.Number(object.keks_rewrapped)
        : 0,
    };
  },

  toJSON(message: RotateMasterKeyResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.keksRewrapped !== 0) {
      obj.keksRewrapped = Math.round(message.keksRewrapped);
    }
    return obj;
  },

  create(base?: DeepPartial<RotateMasterKeyResponse>): RotateMasterKeyResponse {
    return RotateMasterKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RotateMasterKeyResponse>): RotateMasterKeyResponse {
    const message = createBaseRotateMasterKeyResponse();
    message.message = object.message ?? "";
    message.keksRewrapped = object.keksRewrapped ?? 0;
    return message;
  },
};

function createBaseGetSOPSPublicKeyRequest(): GetSOPSPublicKeyRequest {
  return {};
}

export const GetSOPSPublicKeyRequest: MessageFns<GetSOPSPublicKeyRequest> = {
  encode(_: GetSOPSPublicKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSOPSPublicKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSOPSPublicKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetSOPSPublicKeyRequest {
    return {};
  },

  toJSON(_: GetSOPSPublicKeyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetSOPSPublicKeyRequest>): GetSOPSPublicKeyRequest {
    return GetSOPSPublicKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetSOPSPublicKeyRequest>): GetSOPSPublicKeyRequest {
    const message = createBaseGetSOPSPublicKeyRequest();
    return message;
  },
};

function createBaseGetSOPSPublicKeyResponse(): GetSOPSPublicKeyResponse {
  return { publicKey: "", fingerprint: "", createdAt: "", environment: "" };
}

export const GetSOPSPublicKeyResponse: MessageFns<GetSOPSPublicKeyResponse> = {
  encode(message: GetSOPSPublicKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKey !== "") {
      writer.uint32(10).string(message.publicKey);
    }
    if (message.fingerprint !== "") {
      writer.uint32(18).string(message.fingerprint);
    }
    if (message.createdAt !== "") {
      writer.uint32(26).string(message.createdAt);
    }
    if (message.environment !== "") {
      writer.uint32(34).string(message.environment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSOPSPublicKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSOPSPublicKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fingerprint = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.environment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSOPSPublicKeyResponse {
    return {
      publicKey: isSet(object.publicKey)
        ? globalThis.String(object.publicKey)
        : isSet(object.public_key)
        ? globalThis.String(object.public_key)
        : "",
      fingerprint: isSet(object.fingerprint) ? globalThis.String(object.fingerprint) : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      environment: isSet(object.environment) ? globalThis.String(object.environment) : "",
    };
  },

  toJSON(message: GetSOPSPublicKeyResponse): unknown {
    const obj: any = {};
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.fingerprint !== "") {
      obj.fingerprint = message.fingerprint;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.environment !== "") {
      obj.environment = message.environment;
    }
    return obj;
  },

  create(base?: DeepPartial<GetSOPSPublicKeyResponse>): GetSOPSPublicKeyResponse {
    return GetSOPSPublicKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSOPSPublicKeyResponse>): GetSOPSPublicKeyResponse {
    const message = createBaseGetSOPSPublicKeyResponse();
    message.publicKey = object.publicKey ?? "";
    message.fingerprint = object.fingerprint ?? "";
    message.createdAt = object.createdAt ?? "";
    message.environment = object.environment ?? "";
    return message;
  },
};

function createBaseRotateSOPSKeyRequest(): RotateSOPSKeyRequest {
  return {};
}

export const RotateSOPSKeyRequest: MessageFns<RotateSOPSKeyRequest> = {
  encode(_: RotateSOPSKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateSOPSKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateSOPSKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): RotateSOPSKeyRequest {
    return {};
  },

  toJSON(_: RotateSOPSKeyRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<RotateSOPSKeyRequest>): RotateSOPSKeyRequest {
    return RotateSOPSKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<RotateSOPSKeyRequest>): RotateSOPSKeyRequest {
    const message = createBaseRotateSOPSKeyRequest();
    return message;
  },
};

function createBaseRotateSOPSKeyResponse(): RotateSOPSKeyResponse {
  return { newPublicKey: "", newFingerprint: "", oldPublicKey: "", newEnvironment: "" };
}

export const RotateSOPSKeyResponse: MessageFns<RotateSOPSKeyResponse> = {
  encode(message: RotateSOPSKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.newPublicKey !== "") {
      writer.uint32(10).string(message.newPublicKey);
    }
    if (message.newFingerprint !== "") {
      writer.uint32(18).string(message.newFingerprint);
    }
    if (message.oldPublicKey !== "") {
      writer.uint32(26).string(message.oldPublicKey);
    }
    if (message.newEnvironment !== "") {
      writer.uint32(34).string(message.newEnvironment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RotateSOPSKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRotateSOPSKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.newPublicKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newFingerprint = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.oldPublicKey = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.newEnvironment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RotateSOPSKeyResponse {
    return {
      newPublicKey: isSet(object.newPublicKey)
        ? globalThis.String(object.newPublicKey)
        : isSet(object.new_public_key)
        ? globalThis.String(object.new_public_key)
        : "",
      newFingerprint: isSet(object.newFingerprint)
        ? globalThis.String(object.newFingerprint)
        : isSet(object.new_fingerprint)
        ? globalThis.String(object.new_fingerprint)
        : "",
      oldPublicKey: isSet(object.oldPublicKey)
        ? globalThis.String(object.oldPublicKey)
        : isSet(object.old_public_key)
        ? globalThis.String(object.old_public_key)
        : "",
      newEnvironment: isSet(object.newEnvironment)
        ? globalThis.String(object.newEnvironment)
        : isSet(object.new_environment)
        ? globalThis.String(object.new_environment)
        : "",
    };
  },

  toJSON(message: RotateSOPSKeyResponse): unknown {
    const obj: any = {};
    if (message.newPublicKey !== "") {
      obj.newPublicKey = message.newPublicKey;
    }
    if (message.newFingerprint !== "") {
      obj.newFingerprint = message.newFingerprint;
    }
    if (message.oldPublicKey !== "") {
      obj.oldPublicKey = message.oldPublicKey;
    }
    if (message.newEnvironment !== "") {
      obj.newEnvironment = message.newEnvironment;
    }
    return obj;
  },

  create(base?: DeepPartial<RotateSOPSKeyResponse>): RotateSOPSKeyResponse {
    return RotateSOPSKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RotateSOPSKeyResponse>): RotateSOPSKeyResponse {
    const message = createBaseRotateSOPSKeyResponse();
    message.newPublicKey = object.newPublicKey ?? "";
    message.newFingerprint = object.newFingerprint ?? "";
    message.oldPublicKey = object.oldPublicKey ?? "";
    message.newEnvironment = object.newEnvironment ?? "";
    return message;
  },
};

function createBaseListSOPSKeysRequest(): ListSOPSKeysRequest {
  return {};
}

export const ListSOPSKeysRequest: MessageFns<ListSOPSKeysRequest> = {
  encode(_: ListSOPSKeysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSOPSKeysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSOPSKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListSOPSKeysRequest {
    return {};
  },

  toJSON(_: ListSOPSKeysRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListSOPSKeysRequest>): ListSOPSKeysRequest {
    return ListSOPSKeysRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListSOPSKeysRequest>): ListSOPSKeysRequest {
    const message = createBaseListSOPSKeysRequest();
    return message;
  },
};

function createBaseSOPSKeyInfo(): SOPSKeyInfo {
  return { publicKey: "", fingerprint: "", isActive: false, createdAt: "", deactivatedAt: "", environment: "" };
}

export const SOPSKeyInfo: MessageFns<SOPSKeyInfo> = {
  encode(message: SOPSKeyInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKey !== "") {
      writer.uint32(10).string(message.publicKey);
    }
    if (message.fingerprint !== "") {
      writer.uint32(18).string(message.fingerprint);
    }
    if (message.isActive !== false) {
      writer.uint32(24).bool(message.isActive);
    }
    if (message.createdAt !== "") {
      writer.uint32(34).string(message.createdAt);
    }
    if (message.deactivatedAt !== "") {
      writer.uint32(42).string(message.deactivatedAt);
    }
    if (message.environment !== "") {
      writer.uint32(50).string(message.environment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SOPSKeyInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSOPSKeyInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.fingerprint = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deactivatedAt = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.environment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SOPSKeyInfo {
    return {
      publicKey: isSet(object.publicKey)
        ? globalThis.String(object.publicKey)
        : isSet(object.public_key)
        ? globalThis.String(object.public_key)
        : "",
      fingerprint: isSet(object.fingerprint) ? globalThis.String(object.fingerprint) : "",
      isActive: isSet(object.isActive)
        ? globalThis.Boolean(object.isActive)
        : isSet(object.is_active)
        ? globalThis.Boolean(object.is_active)
        : false,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      deactivatedAt: isSet(object.deactivatedAt)
        ? globalThis.String(object.deactivatedAt)
        : isSet(object.deactivated_at)
        ? globalThis.String(object.deactivated_at)
        : "",
      environment: isSet(object.environment) ? globalThis.String(object.environment) : "",
    };
  },

  toJSON(message: SOPSKeyInfo): unknown {
    const obj: any = {};
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    if (message.fingerprint !== "") {
      obj.fingerprint = message.fingerprint;
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.deactivatedAt !== "") {
      obj.deactivatedAt = message.deactivatedAt;
    }
    if (message.environment !== "") {
      obj.environment = message.environment;
    }
    return obj;
  },

  create(base?: DeepPartial<SOPSKeyInfo>): SOPSKeyInfo {
    return SOPSKeyInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SOPSKeyInfo>): SOPSKeyInfo {
    const message = createBaseSOPSKeyInfo();
    message.publicKey = object.publicKey ?? "";
    message.fingerprint = object.fingerprint ?? "";
    message.isActive = object.isActive ?? false;
    message.createdAt = object.createdAt ?? "";
    message.deactivatedAt = object.deactivatedAt ?? "";
    message.environment = object.environment ?? "";
    return message;
  },
};

function createBaseListSOPSKeysResponse(): ListSOPSKeysResponse {
  return { keys: [] };
}

export const ListSOPSKeysResponse: MessageFns<ListSOPSKeysResponse> = {
  encode(message: ListSOPSKeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      SOPSKeyInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListSOPSKeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListSOPSKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keys.push(SOPSKeyInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListSOPSKeysResponse {
    return { keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => SOPSKeyInfo.fromJSON(e)) : [] };
  },

  toJSON(message: ListSOPSKeysResponse): unknown {
    const obj: any = {};
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => SOPSKeyInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListSOPSKeysResponse>): ListSOPSKeysResponse {
    return ListSOPSKeysResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListSOPSKeysResponse>): ListSOPSKeysResponse {
    const message = createBaseListSOPSKeysResponse();
    message.keys = object.keys?.map((e) => SOPSKeyInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBasePruneSOPSKeyRequest(): PruneSOPSKeyRequest {
  return { publicKey: "" };
}

export const PruneSOPSKeyRequest: MessageFns<PruneSOPSKeyRequest> = {
  encode(message: PruneSOPSKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKey !== "") {
      writer.uint32(10).string(message.publicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PruneSOPSKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePruneSOPSKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PruneSOPSKeyRequest {
    return {
      publicKey: isSet(object.publicKey)
        ? globalThis.String(object.publicKey)
        : isSet(object.public_key)
        ? globalThis.String(object.public_key)
        : "",
    };
  },

  toJSON(message: PruneSOPSKeyRequest): unknown {
    const obj: any = {};
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    return obj;
  },

  create(base?: DeepPartial<PruneSOPSKeyRequest>): PruneSOPSKeyRequest {
    return PruneSOPSKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PruneSOPSKeyRequest>): PruneSOPSKeyRequest {
    const message = createBasePruneSOPSKeyRequest();
    message.publicKey = object.publicKey ?? "";
    return message;
  },
};

function createBasePruneSOPSKeyResponse(): PruneSOPSKeyResponse {
  return { message: "" };
}

export const PruneSOPSKeyResponse: MessageFns<PruneSOPSKeyResponse> = {
  encode(message: PruneSOPSKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PruneSOPSKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePruneSOPSKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PruneSOPSKeyResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: PruneSOPSKeyResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<PruneSOPSKeyResponse>): PruneSOPSKeyResponse {
    return PruneSOPSKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PruneSOPSKeyResponse>): PruneSOPSKeyResponse {
    const message = createBasePruneSOPSKeyResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseRegisterRepositoryRequest(): RegisterRepositoryRequest {
  return { name: "", repoUrl: "", branch: "", secretsPath: "", deployKey: new Uint8Array(0), configPath: "" };
}

export const RegisterRepositoryRequest: MessageFns<RegisterRepositoryRequest> = {
  encode(message: RegisterRepositoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.repoUrl !== "") {
      writer.uint32(18).string(message.repoUrl);
    }
    if (message.branch !== "") {
      writer.uint32(26).string(message.branch);
    }
    if (message.secretsPath !== "") {
      writer.uint32(34).string(message.secretsPath);
    }
    if (message.deployKey.length !== 0) {
      writer.uint32(42).bytes(message.deployKey);
    }
    if (message.configPath !== "") {
      writer.uint32(50).string(message.configPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRepositoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRepositoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repoUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.secretsPath = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deployKey = reader.bytes();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.configPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRepositoryRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      repoUrl: isSet(object.repoUrl)
        ? globalThis.String(object.repoUrl)
        : isSet(object.repo_url)
        ? globalThis.String(object.repo_url)
        : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      secretsPath: isSet(object.secretsPath)
        ? globalThis.String(object.secretsPath)
        : isSet(object.secrets_path)
        ? globalThis.String(object.secrets_path)
        : "",
      deployKey: isSet(object.deployKey)
        ? bytesFromBase64(object.deployKey)
        : isSet(object.deploy_key)
        ? bytesFromBase64(object.deploy_key)
        : new Uint8Array(0),
      configPath: isSet(object.configPath)
        ? globalThis.String(object.configPath)
        : isSet(object.config_path)
        ? globalThis.String(object.config_path)
        : "",
    };
  },

  toJSON(message: RegisterRepositoryRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.repoUrl !== "") {
      obj.repoUrl = message.repoUrl;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.secretsPath !== "") {
      obj.secretsPath = message.secretsPath;
    }
    if (message.deployKey.length !== 0) {
      obj.deployKey = base64FromBytes(message.deployKey);
    }
    if (message.configPath !== "") {
      obj.configPath = message.configPath;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterRepositoryRequest>): RegisterRepositoryRequest {
    return RegisterRepositoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterRepositoryRequest>): RegisterRepositoryRequest {
    const message = createBaseRegisterRepositoryRequest();
    message.name = object.name ?? "";
    message.repoUrl = object.repoUrl ?? "";
    message.branch = object.branch ?? "";
    message.secretsPath = object.secretsPath ?? "";
    message.deployKey = object.deployKey ?? new Uint8Array(0);
    message.configPath = object.configPath ?? "";
    return message;
  },
};

function createBaseRegisterRepositoryResponse(): RegisterRepositoryResponse {
  return { id: "", webhookUrl: "", webhookSecret: "" };
}

export const RegisterRepositoryResponse: MessageFns<RegisterRepositoryResponse> = {
  encode(message: RegisterRepositoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.webhookUrl !== "") {
      writer.uint32(18).string(message.webhookUrl);
    }
    if (message.webhookSecret !== "") {
      writer.uint32(26).string(message.webhookSecret);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterRepositoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterRepositoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.webhookUrl = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.webhookSecret = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterRepositoryResponse {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      webhookUrl: isSet(object.webhookUrl)
        ? globalThis.String(object.webhookUrl)
        : isSet(object.webhook_url)
        ? globalThis.String(object.webhook_url)
        : "",
      webhookSecret: isSet(object.webhookSecret)
        ? globalThis.String(object.webhookSecret)
        : isSet(object.webhook_secret)
        ? globalThis.String(object.webhook_secret)
        : "",
    };
  },

  toJSON(message: RegisterRepositoryResponse): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.webhookUrl !== "") {
      obj.webhookUrl = message.webhookUrl;
    }
    if (message.webhookSecret !== "") {
      obj.webhookSecret = message.webhookSecret;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterRepositoryResponse>): RegisterRepositoryResponse {
    return RegisterRepositoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterRepositoryResponse>): RegisterRepositoryResponse {
    const message = createBaseRegisterRepositoryResponse();
    message.id = object.id ?? "";
    message.webhookUrl = object.webhookUrl ?? "";
    message.webhookSecret = object.webhookSecret ?? "";
    return message;
  },
};

function createBaseListRepositoriesRequest(): ListRepositoriesRequest {
  return {};
}

export const ListRepositoriesRequest: MessageFns<ListRepositoriesRequest> = {
  encode(_: ListRepositoriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRepositoriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRepositoriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListRepositoriesRequest {
    return {};
  },

  toJSON(_: ListRepositoriesRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ListRepositoriesRequest>): ListRepositoriesRequest {
    return ListRepositoriesRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ListRepositoriesRequest>): ListRepositoriesRequest {
    const message = createBaseListRepositoriesRequest();
    return message;
  },
};

function createBaseRepositoryInfo(): RepositoryInfo {
  return {
    id: "",
    name: "",
    repoUrl: "",
    branch: "",
    secretsPath: "",
    lastSyncSha: "",
    lastSyncAt: "",
    configPath: "",
  };
}

export const RepositoryInfo: MessageFns<RepositoryInfo> = {
  encode(message: RepositoryInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.repoUrl !== "") {
      writer.uint32(26).string(message.repoUrl);
    }
    if (message.branch !== "") {
      writer.uint32(34).string(message.branch);
    }
    if (message.secretsPath !== "") {
      writer.uint32(42).string(message.secretsPath);
    }
    if (message.lastSyncSha !== "") {
      writer.uint32(50).string(message.lastSyncSha);
    }
    if (message.lastSyncAt !== "") {
      writer.uint32(58).string(message.lastSyncAt);
    }
    if (message.configPath !== "") {
      writer.uint32(66).string(message.configPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RepositoryInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRepositoryInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.repoUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secretsPath = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.lastSyncSha = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastSyncAt = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.configPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RepositoryInfo {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      repoUrl: isSet(object.repoUrl)
        ? globalThis.String(object.repoUrl)
        : isSet(object.repo_url)
        ? globalThis.String(object.repo_url)
        : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      secretsPath: isSet(object.secretsPath)
        ? globalThis.String(object.secretsPath)
        : isSet(object.secrets_path)
        ? globalThis.String(object.secrets_path)
        : "",
      lastSyncSha: isSet(object.lastSyncSha)
        ? globalThis.String(object.lastSyncSha)
        : isSet(object.last_sync_sha)
        ? globalThis.String(object.last_sync_sha)
        : "",
      lastSyncAt: isSet(object.lastSyncAt)
        ? globalThis.String(object.lastSyncAt)
        : isSet(object.last_sync_at)
        ? globalThis.String(object.last_sync_at)
        : "",
      configPath: isSet(object.configPath)
        ? globalThis.String(object.configPath)
        : isSet(object.config_path)
        ? globalThis.String(object.config_path)
        : "",
    };
  },

  toJSON(message: RepositoryInfo): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.repoUrl !== "") {
      obj.repoUrl = message.repoUrl;
    }
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.secretsPath !== "") {
      obj.secretsPath = message.secretsPath;
    }
    if (message.lastSyncSha !== "") {
      obj.lastSyncSha = message.lastSyncSha;
    }
    if (message.lastSyncAt !== "") {
      obj.lastSyncAt = message.lastSyncAt;
    }
    if (message.configPath !== "") {
      obj.configPath = message.configPath;
    }
    return obj;
  },

  create(base?: DeepPartial<RepositoryInfo>): RepositoryInfo {
    return RepositoryInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RepositoryInfo>): RepositoryInfo {
    const message = createBaseRepositoryInfo();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.repoUrl = object.repoUrl ?? "";
    message.branch = object.branch ?? "";
    message.secretsPath = object.secretsPath ?? "";
    message.lastSyncSha = object.lastSyncSha ?? "";
    message.lastSyncAt = object.lastSyncAt ?? "";
    message.configPath = object.configPath ?? "";
    return message;
  },
};

function createBaseListRepositoriesResponse(): ListRepositoriesResponse {
  return { repositories: [] };
}

export const ListRepositoriesResponse: MessageFns<ListRepositoriesResponse> = {
  encode(message: ListRepositoriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.repositories) {
      RepositoryInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRepositoriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRepositoriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.repositories.push(RepositoryInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRepositoriesResponse {
    return {
      repositories: globalThis.Array.isArray(object?.repositories)
        ? object.repositories.map((e: any) => RepositoryInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListRepositoriesResponse): unknown {
    const obj: any = {};
    if (message.repositories?.length) {
      obj.repositories = message.repositories.map((e) => RepositoryInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListRepositoriesResponse>): ListRepositoriesResponse {
    return ListRepositoriesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListRepositoriesResponse>): ListRepositoriesResponse {
    const message = createBaseListRepositoriesResponse();
    message.repositories = object.repositories?.map((e) => RepositoryInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRemoveRepositoryRequest(): RemoveRepositoryRequest {
  return { id: "" };
}

export const RemoveRepositoryRequest: MessageFns<RemoveRepositoryRequest> = {
  encode(message: RemoveRepositoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveRepositoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveRepositoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveRepositoryRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: RemoveRepositoryRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveRepositoryRequest>): RemoveRepositoryRequest {
    return RemoveRepositoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveRepositoryRequest>): RemoveRepositoryRequest {
    const message = createBaseRemoveRepositoryRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseRemoveRepositoryResponse(): RemoveRepositoryResponse {
  return { message: "" };
}

export const RemoveRepositoryResponse: MessageFns<RemoveRepositoryResponse> = {
  encode(message: RemoveRepositoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoveRepositoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRemoveRepositoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RemoveRepositoryResponse {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: RemoveRepositoryResponse): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<RemoveRepositoryResponse>): RemoveRepositoryResponse {
    return RemoveRepositoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RemoveRepositoryResponse>): RemoveRepositoryResponse {
    const message = createBaseRemoveRepositoryResponse();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseTriggerSyncRequest(): TriggerSyncRequest {
  return { id: "" };
}

export const TriggerSyncRequest: MessageFns<TriggerSyncRequest> = {
  encode(message: TriggerSyncRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TriggerSyncRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerSyncRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerSyncRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: TriggerSyncRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<TriggerSyncRequest>): TriggerSyncRequest {
    return TriggerSyncRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TriggerSyncRequest>): TriggerSyncRequest {
    const message = createBaseTriggerSyncRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseTriggerSyncResponse(): TriggerSyncResponse {
  return { secretsAdded: 0, secretsUpdated: 0, secretsDeleted: 0, syncSha: "", errors: [], configsSynced: 0 };
}

export const TriggerSyncResponse: MessageFns<TriggerSyncResponse> = {
  encode(message: TriggerSyncResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretsAdded !== 0) {
      writer.uint32(8).int32(message.secretsAdded);
    }
    if (message.secretsUpdated !== 0) {
      writer.uint32(16).int32(message.secretsUpdated);
    }
    if (message.secretsDeleted !== 0) {
      writer.uint32(24).int32(message.secretsDeleted);
    }
    if (message.syncSha !== "") {
      writer.uint32(34).string(message.syncSha);
    }
    for (const v of message.errors) {
      writer.uint32(42).string(v!);
    }
    if (message.configsSynced !== 0) {
      writer.uint32(48).int32(message.configsSynced);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TriggerSyncResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTriggerSyncResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.secretsAdded = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.secretsUpdated = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.secretsDeleted = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.syncSha = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.errors.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.configsSynced = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TriggerSyncResponse {
    return {
      secretsAdded: isSet(object.secretsAdded)
        ? globalThis.Number(object.secretsAdded)
        : isSet(object.secrets_added)
        ? globalThis.Number(object.secrets_added)
        : 0,
      secretsUpdated: isSet(object.secretsUpdated)
        ? globalThis.Number(object.secretsUpdated)
        : isSet(object.secrets_updated)
        ? globalThis.Number(object.secrets_updated)
        : 0,
      secretsDeleted: isSet(object.secretsDeleted)
        ? globalThis.Number(object.secretsDeleted)
        : isSet(object.secrets_deleted)
        ? globalThis.Number(object.secrets_deleted)
        : 0,
      syncSha: isSet(object.syncSha)
        ? globalThis.String(object.syncSha)
        : isSet(object.sync_sha)
        ? globalThis.String(object.sync_sha)
        : "",
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => globalThis.String(e))
        : [],
      configsSynced: isSet(object.configsSynced)
        ? globalThis.Number(object.configsSynced)
        : isSet(object.configs_synced)
        ? globalThis.Number(object.configs_synced)
        : 0,
    };
  },

  toJSON(message: TriggerSyncResponse): unknown {
    const obj: any = {};
    if (message.secretsAdded !== 0) {
      obj.secretsAdded = Math.round(message.secretsAdded);
    }
    if (message.secretsUpdated !== 0) {
      obj.secretsUpdated = Math.round(message.secretsUpdated);
    }
    if (message.secretsDeleted !== 0) {
      obj.secretsDeleted = Math.round(message.secretsDeleted);
    }
    if (message.syncSha !== "") {
      obj.syncSha = message.syncSha;
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    if (message.configsSynced !== 0) {
      obj.configsSynced = Math.round(message.configsSynced);
    }
    return obj;
  },

  create(base?: DeepPartial<TriggerSyncResponse>): TriggerSyncResponse {
    return TriggerSyncResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TriggerSyncResponse>): TriggerSyncResponse {
    const message = createBaseTriggerSyncResponse();
    message.secretsAdded = object.secretsAdded ?? 0;
    message.secretsUpdated = object.secretsUpdated ?? 0;
    message.secretsDeleted = object.secretsDeleted ?? 0;
    message.syncSha = object.syncSha ?? "";
    message.errors = object.errors?.map((e) => e) || [];
    message.configsSynced = object.configsSynced ?? 0;
    return message;
  },
};

function createBaseSyncBundleChunk(): SyncBundleChunk {
  return { header: undefined, data: undefined };
}

export const SyncBundleChunk: MessageFns<SyncBundleChunk> = {
  encode(message: SyncBundleChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.header !== undefined) {
      SyncBundleHeader.encode(message.header, writer.uint32(10).fork()).join();
    }
    if (message.data !== undefined) {
      writer.uint32(18).bytes(message.data);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncBundleChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncBundleChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.header = SyncBundleHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.data = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncBundleChunk {
    return {
      header: isSet(object.header) ? SyncBundleHeader.fromJSON(object.header) : undefined,
      data: isSet(object.data) ? bytesFromBase64(object.data) : undefined,
    };
  },

  toJSON(message: SyncBundleChunk): unknown {
    const obj: any = {};
    if (message.header !== undefined) {
      obj.header = SyncBundleHeader.toJSON(message.header);
    }
    if (message.data !== undefined) {
      obj.data = base64FromBytes(message.data);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncBundleChunk>): SyncBundleChunk {
    return SyncBundleChunk.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncBundleChunk>): SyncBundleChunk {
    const message = createBaseSyncBundleChunk();
    message.header = (object.header !== undefined && object.header !== null)
      ? SyncBundleHeader.fromPartial(object.header)
      : undefined;
    message.data = object.data ?? undefined;
    return message;
  },
};

function createBaseSyncBundleHeader(): SyncBundleHeader {
  return { secretsPath: "", headSha: "", configPath: "" };
}

export const SyncBundleHeader: MessageFns<SyncBundleHeader> = {
  encode(message: SyncBundleHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretsPath !== "") {
      writer.uint32(10).string(message.secretsPath);
    }
    if (message.headSha !== "") {
      writer.uint32(18).string(message.headSha);
    }
    if (message.configPath !== "") {
      writer.uint32(26).string(message.configPath);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncBundleHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncBundleHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.secretsPath = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.headSha = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.configPath = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncBundleHeader {
    return {
      secretsPath: isSet(object.secretsPath)
        ? globalThis.String(object.secretsPath)
        : isSet(object.secrets_path)
        ? globalThis.String(object.secrets_path)
        : "",
      headSha: isSet(object.headSha)
        ? globalThis.String(object.headSha)
        : isSet(object.head_sha)
        ? globalThis.String(object.head_sha)
        : "",
      configPath: isSet(object.configPath)
        ? globalThis.String(object.configPath)
        : isSet(object.config_path)
        ? globalThis.String(object.config_path)
        : "",
    };
  },

  toJSON(message: SyncBundleHeader): unknown {
    const obj: any = {};
    if (message.secretsPath !== "") {
      obj.secretsPath = message.secretsPath;
    }
    if (message.headSha !== "") {
      obj.headSha = message.headSha;
    }
    if (message.configPath !== "") {
      obj.configPath = message.configPath;
    }
    return obj;
  },

  create(base?: DeepPartial<SyncBundleHeader>): SyncBundleHeader {
    return SyncBundleHeader.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncBundleHeader>): SyncBundleHeader {
    const message = createBaseSyncBundleHeader();
    message.secretsPath = object.secretsPath ?? "";
    message.headSha = object.headSha ?? "";
    message.configPath = object.configPath ?? "";
    return message;
  },
};

function createBaseSyncBundleResponse(): SyncBundleResponse {
  return { secretsAdded: 0, secretsUpdated: 0, secretsDeleted: 0, syncSha: "", errors: [], configsSynced: 0 };
}

export const SyncBundleResponse: MessageFns<SyncBundleResponse> = {
  encode(message: SyncBundleResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secretsAdded !== 0) {
      writer.uint32(8).int32(message.secretsAdded);
    }
    if (message.secretsUpdated !== 0) {
      writer.uint32(16).int32(message.secretsUpdated);
    }
    if (message.secretsDeleted !== 0) {
      writer.uint32(24).int32(message.secretsDeleted);
    }
    if (message.syncSha !== "") {
      writer.uint32(34).string(message.syncSha);
    }
    for (const v of message.errors) {
      writer.uint32(42).string(v!);
    }
    if (message.configsSynced !== 0) {
      writer.uint32(48).int32(message.configsSynced);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SyncBundleResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSyncBundleResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.secretsAdded = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.secretsUpdated = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.secretsDeleted = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.syncSha = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.errors.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.configsSynced = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SyncBundleResponse {
    return {
      secretsAdded: isSet(object.secretsAdded)
        ? globalThis.Number(object.secretsAdded)
        : isSet(object.secrets_added)
        ? globalThis.Number(object.secrets_added)
        : 0,
      secretsUpdated: isSet(object.secretsUpdated)
        ? globalThis.Number(object.secretsUpdated)
        : isSet(object.secrets_updated)
        ? globalThis.Number(object.secrets_updated)
        : 0,
      secretsDeleted: isSet(object.secretsDeleted)
        ? globalThis.Number(object.secretsDeleted)
        : isSet(object.secrets_deleted)
        ? globalThis.Number(object.secrets_deleted)
        : 0,
      syncSha: isSet(object.syncSha)
        ? globalThis.String(object.syncSha)
        : isSet(object.sync_sha)
        ? globalThis.String(object.sync_sha)
        : "",
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => globalThis.String(e))
        : [],
      configsSynced: isSet(object.configsSynced)
        ? globalThis.Number(object.configsSynced)
        : isSet(object.configs_synced)
        ? globalThis.Number(object.configs_synced)
        : 0,
    };
  },

  toJSON(message: SyncBundleResponse): unknown {
    const obj: any = {};
    if (message.secretsAdded !== 0) {
      obj.secretsAdded = Math.round(message.secretsAdded);
    }
    if (message.secretsUpdated !== 0) {
      obj.secretsUpdated = Math.round(message.secretsUpdated);
    }
    if (message.secretsDeleted !== 0) {
      obj.secretsDeleted = Math.round(message.secretsDeleted);
    }
    if (message.syncSha !== "") {
      obj.syncSha = message.syncSha;
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    if (message.configsSynced !== 0) {
      obj.configsSynced = Math.round(message.configsSynced);
    }
    return obj;
  },

  create(base?: DeepPartial<SyncBundleResponse>): SyncBundleResponse {
    return SyncBundleResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SyncBundleResponse>): SyncBundleResponse {
    const message = createBaseSyncBundleResponse();
    message.secretsAdded = object.secretsAdded ?? 0;
    message.secretsUpdated = object.secretsUpdated ?? 0;
    message.secretsDeleted = object.secretsDeleted ?? 0;
    message.syncSha = object.syncSha ?? "";
    message.errors = object.errors?.map((e) => e) || [];
    message.configsSynced = object.configsSynced ?? 0;
    return message;
  },
};

/** AdminService handles operator lifecycle operations: unsealing, sealing, and status. */
export type AdminServiceService = typeof AdminServiceService;
export const AdminServiceService = {
  unsealKey: {
    path: "/admin.v1.AdminService/UnsealKey" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UnsealKeyRequest): Buffer => Buffer.from(UnsealKeyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UnsealKeyRequest => UnsealKeyRequest.decode(value),
    responseSerialize: (value: UnsealKeyResponse): Buffer => Buffer.from(UnsealKeyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UnsealKeyResponse => UnsealKeyResponse.decode(value),
  },
  unsealShare: {
    path: "/admin.v1.AdminService/UnsealShare" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: UnsealShareRequest): Buffer => Buffer.from(UnsealShareRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): UnsealShareRequest => UnsealShareRequest.decode(value),
    responseSerialize: (value: UnsealShareResponse): Buffer => Buffer.from(UnsealShareResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): UnsealShareResponse => UnsealShareResponse.decode(value),
  },
  seal: {
    path: "/admin.v1.AdminService/Seal" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SealRequest): Buffer => Buffer.from(SealRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SealRequest => SealRequest.decode(value),
    responseSerialize: (value: SealResponse): Buffer => Buffer.from(SealResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SealResponse => SealResponse.decode(value),
  },
  status: {
    path: "/admin.v1.AdminService/Status" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: StatusRequest): Buffer => Buffer.from(StatusRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): StatusRequest => StatusRequest.decode(value),
    responseSerialize: (value: StatusResponse): Buffer => Buffer.from(StatusResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): StatusResponse => StatusResponse.decode(value),
  },
  /**
   * Key-encryption-key (KEK) lifecycle. Rotating the KEK re-wraps every
   * secret's DEK without re-encrypting any secret's ciphertext blob.
   */
  rotateKek: {
    path: "/admin.v1.AdminService/RotateKEK" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RotateKEKRequest): Buffer => Buffer.from(RotateKEKRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RotateKEKRequest => RotateKEKRequest.decode(value),
    responseSerialize: (value: RotateKEKResponse): Buffer => Buffer.from(RotateKEKResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RotateKEKResponse => RotateKEKResponse.decode(value),
  },
  listKeKs: {
    path: "/admin.v1.AdminService/ListKEKs" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListKEKsRequest): Buffer => Buffer.from(ListKEKsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListKEKsRequest => ListKEKsRequest.decode(value),
    responseSerialize: (value: ListKEKsResponse): Buffer => Buffer.from(ListKEKsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListKEKsResponse => ListKEKsResponse.decode(value),
  },
  pruneKek: {
    path: "/admin.v1.AdminService/PruneKEK" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PruneKEKRequest): Buffer => Buffer.from(PruneKEKRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PruneKEKRequest => PruneKEKRequest.decode(value),
    responseSerialize: (value: PruneKEKResponse): Buffer => Buffer.from(PruneKEKResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PruneKEKResponse => PruneKEKResponse.decode(value),
  },
  /**
   * RotateMasterKey re-wraps every KEK (and the key-check value) under a new
   * master key supplied by the operator, then adopts it as the active master
   * key. It does not touch secrets or their DEKs. The caller is responsible
   * for redistributing the new key material to keyholders (Shamir) or
   * updating the Kubernetes Secret (cluster-native auto-unseal) afterward.
   */
  rotateMasterKey: {
    path: "/admin.v1.AdminService/RotateMasterKey" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RotateMasterKeyRequest): Buffer =>
      Buffer.from(RotateMasterKeyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RotateMasterKeyRequest => RotateMasterKeyRequest.decode(value),
    responseSerialize: (value: RotateMasterKeyResponse): Buffer =>
      Buffer.from(RotateMasterKeyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RotateMasterKeyResponse => RotateMasterKeyResponse.decode(value),
  },
} as const;

export interface AdminServiceServer extends UntypedServiceImplementation {
  unsealKey: handleUnaryCall<UnsealKeyRequest, UnsealKeyResponse>;
  unsealShare: handleUnaryCall<UnsealShareRequest, UnsealShareResponse>;
  seal: handleUnaryCall<SealRequest, SealResponse>;
  status: handleUnaryCall<StatusRequest, StatusResponse>;
  /**
   * Key-encryption-key (KEK) lifecycle. Rotating the KEK re-wraps every
   * secret's DEK without re-encrypting any secret's ciphertext blob.
   */
  rotateKek: handleUnaryCall<RotateKEKRequest, RotateKEKResponse>;
  listKeKs: handleUnaryCall<ListKEKsRequest, ListKEKsResponse>;
  pruneKek: handleUnaryCall<PruneKEKRequest, PruneKEKResponse>;
  /**
   * RotateMasterKey re-wraps every KEK (and the key-check value) under a new
   * master key supplied by the operator, then adopts it as the active master
   * key. It does not touch secrets or their DEKs. The caller is responsible
   * for redistributing the new key material to keyholders (Shamir) or
   * updating the Kubernetes Secret (cluster-native auto-unseal) afterward.
   */
  rotateMasterKey: handleUnaryCall<RotateMasterKeyRequest, RotateMasterKeyResponse>;
}

export interface AdminServiceClient extends Client {
  unsealKey(
    request: UnsealKeyRequest,
    callback: (error: ServiceError | null, response: UnsealKeyResponse) => void,
  ): ClientUnaryCall;
  unsealKey(
    request: UnsealKeyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UnsealKeyResponse) => void,
  ): ClientUnaryCall;
  unsealKey(
    request: UnsealKeyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UnsealKeyResponse) => void,
  ): ClientUnaryCall;
  unsealShare(
    request: UnsealShareRequest,
    callback: (error: ServiceError | null, response: UnsealShareResponse) => void,
  ): ClientUnaryCall;
  unsealShare(
    request: UnsealShareRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: UnsealShareResponse) => void,
  ): ClientUnaryCall;
  unsealShare(
    request: UnsealShareRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: UnsealShareResponse) => void,
  ): ClientUnaryCall;
  seal(request: SealRequest, callback: (error: ServiceError | null, response: SealResponse) => void): ClientUnaryCall;
  seal(
    request: SealRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SealResponse) => void,
  ): ClientUnaryCall;
  seal(
    request: SealRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SealResponse) => void,
  ): ClientUnaryCall;
  status(
    request: StatusRequest,
    callback: (error: ServiceError | null, response: StatusResponse) => void,
  ): ClientUnaryCall;
  status(
    request: StatusRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StatusResponse) => void,
  ): ClientUnaryCall;
  status(
    request: StatusRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StatusResponse) => void,
  ): ClientUnaryCall;
  /**
   * Key-encryption-key (KEK) lifecycle. Rotating the KEK re-wraps every
   * secret's DEK without re-encrypting any secret's ciphertext blob.
   */
  rotateKek(
    request: RotateKEKRequest,
    callback: (error: ServiceError | null, response: RotateKEKResponse) => void,
  ): ClientUnaryCall;
  rotateKek(
    request: RotateKEKRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RotateKEKResponse) => void,
  ): ClientUnaryCall;
  rotateKek(
    request: RotateKEKRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RotateKEKResponse) => void,
  ): ClientUnaryCall;
  listKeKs(
    request: ListKEKsRequest,
    callback: (error: ServiceError | null, response: ListKEKsResponse) => void,
  ): ClientUnaryCall;
  listKeKs(
    request: ListKEKsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListKEKsResponse) => void,
  ): ClientUnaryCall;
  listKeKs(
    request: ListKEKsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListKEKsResponse) => void,
  ): ClientUnaryCall;
  pruneKek(
    request: PruneKEKRequest,
    callback: (error: ServiceError | null, response: PruneKEKResponse) => void,
  ): ClientUnaryCall;
  pruneKek(
    request: PruneKEKRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PruneKEKResponse) => void,
  ): ClientUnaryCall;
  pruneKek(
    request: PruneKEKRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PruneKEKResponse) => void,
  ): ClientUnaryCall;
  /**
   * RotateMasterKey re-wraps every KEK (and the key-check value) under a new
   * master key supplied by the operator, then adopts it as the active master
   * key. It does not touch secrets or their DEKs. The caller is responsible
   * for redistributing the new key material to keyholders (Shamir) or
   * updating the Kubernetes Secret (cluster-native auto-unseal) afterward.
   */
  rotateMasterKey(
    request: RotateMasterKeyRequest,
    callback: (error: ServiceError | null, response: RotateMasterKeyResponse) => void,
  ): ClientUnaryCall;
  rotateMasterKey(
    request: RotateMasterKeyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RotateMasterKeyResponse) => void,
  ): ClientUnaryCall;
  rotateMasterKey(
    request: RotateMasterKeyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RotateMasterKeyResponse) => void,
  ): ClientUnaryCall;
}

export const AdminServiceClient = makeGenericClientConstructor(
  AdminServiceService,
  "admin.v1.AdminService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): AdminServiceClient;
  service: typeof AdminServiceService;
  serviceName: string;
};

/** GitOpsService manages SOPS key lifecycle and GitHub repository sync configuration. */
export type GitOpsServiceService = typeof GitOpsServiceService;
export const GitOpsServiceService = {
  /** SOPS age key management. */
  getSopsPublicKey: {
    path: "/admin.v1.GitOpsService/GetSOPSPublicKey" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetSOPSPublicKeyRequest): Buffer =>
      Buffer.from(GetSOPSPublicKeyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetSOPSPublicKeyRequest => GetSOPSPublicKeyRequest.decode(value),
    responseSerialize: (value: GetSOPSPublicKeyResponse): Buffer =>
      Buffer.from(GetSOPSPublicKeyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetSOPSPublicKeyResponse => GetSOPSPublicKeyResponse.decode(value),
  },
  rotateSopsKey: {
    path: "/admin.v1.GitOpsService/RotateSOPSKey" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RotateSOPSKeyRequest): Buffer => Buffer.from(RotateSOPSKeyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RotateSOPSKeyRequest => RotateSOPSKeyRequest.decode(value),
    responseSerialize: (value: RotateSOPSKeyResponse): Buffer =>
      Buffer.from(RotateSOPSKeyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RotateSOPSKeyResponse => RotateSOPSKeyResponse.decode(value),
  },
  listSopsKeys: {
    path: "/admin.v1.GitOpsService/ListSOPSKeys" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListSOPSKeysRequest): Buffer => Buffer.from(ListSOPSKeysRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListSOPSKeysRequest => ListSOPSKeysRequest.decode(value),
    responseSerialize: (value: ListSOPSKeysResponse): Buffer =>
      Buffer.from(ListSOPSKeysResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListSOPSKeysResponse => ListSOPSKeysResponse.decode(value),
  },
  pruneSopsKey: {
    path: "/admin.v1.GitOpsService/PruneSOPSKey" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: PruneSOPSKeyRequest): Buffer => Buffer.from(PruneSOPSKeyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PruneSOPSKeyRequest => PruneSOPSKeyRequest.decode(value),
    responseSerialize: (value: PruneSOPSKeyResponse): Buffer =>
      Buffer.from(PruneSOPSKeyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PruneSOPSKeyResponse => PruneSOPSKeyResponse.decode(value),
  },
  /** GitHub repository registration and sync. */
  registerRepository: {
    path: "/admin.v1.GitOpsService/RegisterRepository" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RegisterRepositoryRequest): Buffer =>
      Buffer.from(RegisterRepositoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RegisterRepositoryRequest => RegisterRepositoryRequest.decode(value),
    responseSerialize: (value: RegisterRepositoryResponse): Buffer =>
      Buffer.from(RegisterRepositoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RegisterRepositoryResponse => RegisterRepositoryResponse.decode(value),
  },
  listRepositories: {
    path: "/admin.v1.GitOpsService/ListRepositories" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ListRepositoriesRequest): Buffer =>
      Buffer.from(ListRepositoriesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListRepositoriesRequest => ListRepositoriesRequest.decode(value),
    responseSerialize: (value: ListRepositoriesResponse): Buffer =>
      Buffer.from(ListRepositoriesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListRepositoriesResponse => ListRepositoriesResponse.decode(value),
  },
  removeRepository: {
    path: "/admin.v1.GitOpsService/RemoveRepository" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: RemoveRepositoryRequest): Buffer =>
      Buffer.from(RemoveRepositoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): RemoveRepositoryRequest => RemoveRepositoryRequest.decode(value),
    responseSerialize: (value: RemoveRepositoryResponse): Buffer =>
      Buffer.from(RemoveRepositoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): RemoveRepositoryResponse => RemoveRepositoryResponse.decode(value),
  },
  triggerSync: {
    path: "/admin.v1.GitOpsService/TriggerSync" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: TriggerSyncRequest): Buffer => Buffer.from(TriggerSyncRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): TriggerSyncRequest => TriggerSyncRequest.decode(value),
    responseSerialize: (value: TriggerSyncResponse): Buffer => Buffer.from(TriggerSyncResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): TriggerSyncResponse => TriggerSyncResponse.decode(value),
  },
  /**
   * Local bundle sync: push a tar.gz of a local git working tree directly to
   * signet without requiring a remote repository. The first chunk must be a
   * SyncBundleHeader; subsequent chunks carry the raw archive bytes.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   */
  syncBundle: {
    path: "/admin.v1.GitOpsService/SyncBundle" as const,
    requestStream: true as const,
    responseStream: false as const,
    requestSerialize: (value: SyncBundleChunk): Buffer => Buffer.from(SyncBundleChunk.encode(value).finish()),
    requestDeserialize: (value: Buffer): SyncBundleChunk => SyncBundleChunk.decode(value),
    responseSerialize: (value: SyncBundleResponse): Buffer => Buffer.from(SyncBundleResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SyncBundleResponse => SyncBundleResponse.decode(value),
  },
} as const;

export interface GitOpsServiceServer extends UntypedServiceImplementation {
  /** SOPS age key management. */
  getSopsPublicKey: handleUnaryCall<GetSOPSPublicKeyRequest, GetSOPSPublicKeyResponse>;
  rotateSopsKey: handleUnaryCall<RotateSOPSKeyRequest, RotateSOPSKeyResponse>;
  listSopsKeys: handleUnaryCall<ListSOPSKeysRequest, ListSOPSKeysResponse>;
  pruneSopsKey: handleUnaryCall<PruneSOPSKeyRequest, PruneSOPSKeyResponse>;
  /** GitHub repository registration and sync. */
  registerRepository: handleUnaryCall<RegisterRepositoryRequest, RegisterRepositoryResponse>;
  listRepositories: handleUnaryCall<ListRepositoriesRequest, ListRepositoriesResponse>;
  removeRepository: handleUnaryCall<RemoveRepositoryRequest, RemoveRepositoryResponse>;
  triggerSync: handleUnaryCall<TriggerSyncRequest, TriggerSyncResponse>;
  /**
   * Local bundle sync: push a tar.gz of a local git working tree directly to
   * signet without requiring a remote repository. The first chunk must be a
   * SyncBundleHeader; subsequent chunks carry the raw archive bytes.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   */
  syncBundle: handleClientStreamingCall<SyncBundleChunk, SyncBundleResponse>;
}

export interface GitOpsServiceClient extends Client {
  /** SOPS age key management. */
  getSopsPublicKey(
    request: GetSOPSPublicKeyRequest,
    callback: (error: ServiceError | null, response: GetSOPSPublicKeyResponse) => void,
  ): ClientUnaryCall;
  getSopsPublicKey(
    request: GetSOPSPublicKeyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetSOPSPublicKeyResponse) => void,
  ): ClientUnaryCall;
  getSopsPublicKey(
    request: GetSOPSPublicKeyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetSOPSPublicKeyResponse) => void,
  ): ClientUnaryCall;
  rotateSopsKey(
    request: RotateSOPSKeyRequest,
    callback: (error: ServiceError | null, response: RotateSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  rotateSopsKey(
    request: RotateSOPSKeyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RotateSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  rotateSopsKey(
    request: RotateSOPSKeyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RotateSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  listSopsKeys(
    request: ListSOPSKeysRequest,
    callback: (error: ServiceError | null, response: ListSOPSKeysResponse) => void,
  ): ClientUnaryCall;
  listSopsKeys(
    request: ListSOPSKeysRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListSOPSKeysResponse) => void,
  ): ClientUnaryCall;
  listSopsKeys(
    request: ListSOPSKeysRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListSOPSKeysResponse) => void,
  ): ClientUnaryCall;
  pruneSopsKey(
    request: PruneSOPSKeyRequest,
    callback: (error: ServiceError | null, response: PruneSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  pruneSopsKey(
    request: PruneSOPSKeyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PruneSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  pruneSopsKey(
    request: PruneSOPSKeyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PruneSOPSKeyResponse) => void,
  ): ClientUnaryCall;
  /** GitHub repository registration and sync. */
  registerRepository(
    request: RegisterRepositoryRequest,
    callback: (error: ServiceError | null, response: RegisterRepositoryResponse) => void,
  ): ClientUnaryCall;
  registerRepository(
    request: RegisterRepositoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RegisterRepositoryResponse) => void,
  ): ClientUnaryCall;
  registerRepository(
    request: RegisterRepositoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RegisterRepositoryResponse) => void,
  ): ClientUnaryCall;
  listRepositories(
    request: ListRepositoriesRequest,
    callback: (error: ServiceError | null, response: ListRepositoriesResponse) => void,
  ): ClientUnaryCall;
  listRepositories(
    request: ListRepositoriesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListRepositoriesResponse) => void,
  ): ClientUnaryCall;
  listRepositories(
    request: ListRepositoriesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListRepositoriesResponse) => void,
  ): ClientUnaryCall;
  removeRepository(
    request: RemoveRepositoryRequest,
    callback: (error: ServiceError | null, response: RemoveRepositoryResponse) => void,
  ): ClientUnaryCall;
  removeRepository(
    request: RemoveRepositoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RemoveRepositoryResponse) => void,
  ): ClientUnaryCall;
  removeRepository(
    request: RemoveRepositoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RemoveRepositoryResponse) => void,
  ): ClientUnaryCall;
  triggerSync(
    request: TriggerSyncRequest,
    callback: (error: ServiceError | null, response: TriggerSyncResponse) => void,
  ): ClientUnaryCall;
  triggerSync(
    request: TriggerSyncRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: TriggerSyncResponse) => void,
  ): ClientUnaryCall;
  triggerSync(
    request: TriggerSyncRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: TriggerSyncResponse) => void,
  ): ClientUnaryCall;
  /**
   * Local bundle sync: push a tar.gz of a local git working tree directly to
   * signet without requiring a remote repository. The first chunk must be a
   * SyncBundleHeader; subsequent chunks carry the raw archive bytes.
   * buf:lint:ignore RPC_REQUEST_STANDARD_NAME
   */
  syncBundle(
    callback: (error: ServiceError | null, response: SyncBundleResponse) => void,
  ): ClientWritableStream<SyncBundleChunk>;
  syncBundle(
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SyncBundleResponse) => void,
  ): ClientWritableStream<SyncBundleChunk>;
  syncBundle(
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SyncBundleResponse) => void,
  ): ClientWritableStream<SyncBundleChunk>;
  syncBundle(
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SyncBundleResponse) => void,
  ): ClientWritableStream<SyncBundleChunk>;
}

export const GitOpsServiceClient = makeGenericClientConstructor(
  GitOpsServiceService,
  "admin.v1.GitOpsService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): GitOpsServiceClient;
  service: typeof GitOpsServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
